PKGBUILD for srclib tool
------------------------

Installation file for srclib tool for arch linux.

Install with yaourt
===================

```
  yaourt -S srclib
```

Install manually
================

```
  git clone git@github.com:shagabutdinov/srclib-pkgbuild.git
  cd srclib-pkgbuild
  makepkg
  pacman -U *.xz
```